RewriteEngine On
RewriteBase /api/conectarbot/v1/
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]

# Evita listar el directorio
Options -Indexes
