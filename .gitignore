# Ignore cache outputs for link-check
.cache/*.json

# Keep python helper scripts in .cache tracked
!.cache/*.py
.DS_Store
**/.DS_Store
img/surgeon/*_files/
img/surgeon/**/Fotos y videos de Instagram_files/
*.heic
.env
admin/include/conexion.local.php
admin/include/email_credentials.php
img/surgeon/*Instagram*
# Logs
admin/logs/*.log
admin/img/perfil/*.pdf
.vscode/
.vscode/sftp.json
# Ignore workspace settings to avoid committing local interpreter path
.vscode/settings.json

# Stripe credentials — never commit real keys
config/stripe.php
